version: '3.8'

services:
  backend:
    build: .
    container_name: home-website-backend
    ports:
      # Format is HOST_PORT:CONTAINER_PORT. Update if needed.
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
